% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{fit_result}
\alias{fit_result}
\title{The fitter result contract}
\usage{
fit_result(
  estimate = NA_real_,
  se = NA_real_,
  p_value = NA_real_,
  ci_lower = NULL,
  ci_upper = NULL,
  df = Inf,
  converged = TRUE,
  level = 0.95
)
}
\arguments{
\item{estimate}{Numeric point estimate of the estimand.}

\item{se}{Numeric standard error.}

\item{p_value}{Numeric p-value for the null that the estimand is
zero.}

\item{ci_lower, ci_upper}{Numeric confidence limits. Computed from
\code{estimate}, \code{se} and \code{df} when not supplied.}

\item{df}{Numeric denominator degrees of freedom, or \code{Inf} for a
normal reference.}

\item{converged}{Logical.}

\item{level}{Numeric confidence level used when limits are derived.}
}
\value{
An object of class \code{fit_result}, a list with
\describe{
\item{estimate}{the point estimate}
\item{se}{the standard error}
\item{p_value}{the p-value}
\item{ci_lower, ci_upper}{the confidence limits; when either was
not supplied, \strong{both} are recomputed as
\verb{estimate +/- crit * se}, with \code{crit} a \code{t} quantile on \code{df}
degrees of freedom, or a normal quantile when \code{df} is
infinite}
\item{df}{the denominator degrees of freedom used}
\item{converged}{logical convergence flag; non-converged
replicates are excluded by \code{\link[=compute_performance]{compute_performance()}}}
\item{level}{the confidence level}
}
These eight names are the whole contract. Of them,
\code{\link[=run_simulation]{run_simulation()}} reads six -- \code{estimate}, \code{se}, \code{p_value},
\code{ci_lower}, \code{ci_upper} and \code{converged} -- from every fitter, and
nothing else; \code{df} and \code{level} are retained for the fitter's own
reporting.
}
\description{
Every analysis method must return this shape, so that competing
methods are interchangeable downstream and performance measures need
no knowledge of the fitter.
}
\seealso{
\code{\link[=null_fit]{null_fit()}} for the non-convergence case, \code{\link[=fit_ancova]{fit_ancova()}}
for a reference fitter satisfying this contract, and
\code{\link[=run_simulation]{run_simulation()}}, which consumes it.
}
