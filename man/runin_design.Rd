% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schedule.R
\name{runin_design}
\alias{runin_design}
\title{Build the fixed-effect design columns for a run-in trial}
\usage{
runin_design(
  schedule,
  arm,
  hinge = TRUE,
  reference = NULL,
  common_close = c("revert", "retain")
)
}
\arguments{
\item{schedule}{Either a \code{trial_schedule} from \code{\link[=trial_schedule]{trial_schedule()}},
giving every subject the same visit grid, or a
\code{realized_schedule} from \code{\link[=realize_schedule]{realize_schedule()}}, giving each
subject the grid their enrollment date and the common close-out
allow. The second is the staggered-accrual path, and produces a
ragged design with differing numbers of rows per subject.}

\item{arm}{Arm allocation, one entry per subject, of length \code{n}.
Any vector \code{factor()} accepts: a factor is used as given, and
anything else (integer, character, logical) is coerced with
\code{factor()}. Arbitrarily many levels are supported, not just two,
so a three-arm dose-ranging design is written by supplying three
levels. The level ordering matters only through \code{reference}, which
defaults to the first level; every other level gets its own
treatment column.}

\item{hinge}{Logical, length 1 or one entry per arm level. Whether
each arm's mean profile changes slope at randomization. \code{FALSE}
for the reference arm constrains its post-randomization slope to
equal the run-in slope (\code{beta = delta}), which makes the run-in
observations directly informative about the control slope. See
Details.}

\item{reference}{The arm level treated as the reference (control).
Defaults to the first sorted level.}

\item{common_close}{Character. \code{"revert"} (the default) sets the
treatment column to zero during the common close, following
\code{res/04-runin-power-analysis}. \code{"retain"} holds the accumulated
treatment contribution constant after the last on-treatment visit.}
}
\value{
A data frame with one row per subject-visit and columns as
below. From a \code{trial_schedule} there are \code{n * nrow(schedule)}
rows, subjects varying slowest and visits fastest. From a
\code{realized_schedule} the frame is ragged -- each subject keeps the
visits their enrollment date allowed -- and the row order is that
of the realized schedule.

Note when analyzing a ragged design: \code{fit_ancova()} defaults to
\code{max(dat$time)}, which under staggered accrual only the earliest
enrollees reach. Pass \code{visit_time} explicitly, normally the last
nominal visit of the original schedule, or the fit will be based
on a handful of subjects and may return \code{\link[=null_fit]{null_fit()}}.
\describe{
\item{id}{subject index. From a \code{trial_schedule}, \code{1} to \code{n} in
the order \code{arm} was given; from a \code{realized_schedule}, the
\code{id} of that schedule, with \code{arm} matched to it by position}
\item{arm}{the arm factor, recycled across that subject's
visits}
\item{index, time, phase}{copied from \code{schedule}}
\item{x_slope}{the common slope column, equal to \code{time} on every
visit}
\item{x_hinge}{\code{h * time}, the reference arm's post-randomization
slope increment. Present only when the reference arm is hinged
\strong{and} the schedule has a run-in phase (\code{J0 > 0}). With
\code{J0 = 0} the column would be aliased with \code{x_slope}, since
\code{h} differs from \code{1} only at the randomization visit where
\code{time} is zero, so it is omitted rather than handed over as a
rank-deficient design.}
\item{x_trt_\if{html}{\out{<level>}}}{one column per non-reference arm level that
is hinged, holding the treatment-phase time for subjects in
that arm and zero otherwise. Non-hinged arms are skipped, so
there are as many such columns as there are hinged
non-reference levels, not one per non-reference level.}
}
}
\description{
Constructs the slope columns of the model
\deqn{Y_{ij} = \alpha + a_i + (\delta (1 - h_j) + \beta h_j +
  \gamma h_j g_i + b_i) t_j + w_{ij}}{Y_ij = alpha + a_i +
  (delta (1 - h_j) + beta h_j + gamma h_j g_i + b_i) t_j + w_ij}
expanded over subjects, where \eqn{h_j} is the post-randomization
indicator, \eqn{g_i} the arm indicator, \eqn{t_j} the visit time,
\eqn{a_i} and \eqn{b_i} subject-level random intercept and slope,
and \eqn{w_{ij}}{w_ij} residual error. The returned columns carry the
coefficients \eqn{\delta} (\code{x_slope}, the pre-randomization
slope), \eqn{\beta} (\code{x_hinge}, the reference arm's change of
slope at randomization), and \eqn{\gamma} (\verb{x_trt_<level>}, the
treatment effect on the rate of change).
}
\details{
The \code{common_close} argument selects how the treatment column behaves
after the last on-treatment visit. The two conventions are not
equivalent and give different estimands; see Details.

Under \code{"revert"} the treatment indicator is replaced by
\verb{g_i * I(1 <= j <= J1)}, so the mean for a treated subject returns to
the control trajectory at the start of the common close. This is a
discontinuity in the mean: the accumulated benefit \verb{gamma * t_\{J1\}}
is removed at once. It is the convention under which the closed-form
variance results of the companion compendium were derived, and it
makes the common-close visits informative about \code{beta} and the
variance components while contributing nothing to \code{gamma}.

Under \code{"retain"} the treatment column is held at its value at the
last on-treatment visit, so both arms progress with slope \code{beta}
thereafter while the treated arm keeps its accumulated advantage.
This is the more common clinical reading of an off-treatment
follow-up. It is offered because the choice is a modeling decision
rather than a detail, and it should be made explicitly.

The slope for a subject in arm \code{k} is
\itemize{
\item run-in (\code{j <= 0}): \code{delta}, common to all arms;
\item treatment (\verb{1 <= j <= J1}): \code{delta + theta_ref + theta_k};
\item common close (\code{j > J1}): \code{delta + theta_ref}, all arms.
}
\code{theta_ref} is the reference arm's slope change at randomization and
is dropped when \code{hinge} is \code{FALSE} for the reference, constraining
that arm to a single straight line through baseline. \code{theta_k} is
arm \code{k}'s increment over the reference and is the treatment effect
on the rate of change. This is a reparameterization of the
\code{delta}/\code{beta}/\code{gamma} form: \code{theta_ref = beta - delta} and
\code{theta_k = gamma}, spanning the same column space when every arm is
hinged.
}
