% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schedule.R
\name{runin_design}
\alias{runin_design}
\title{Build the fixed-effect design columns for a run-in trial}
\usage{
runin_design(
  schedule,
  arm,
  hinge = TRUE,
  reference = NULL,
  common_close = c("revert", "retain")
)
}
\arguments{
\item{schedule}{A \code{trial_schedule}.}

\item{arm}{Integer or logical vector of length \code{n}, the treatment
indicator \code{g_i} (1 = treatment, 0 = control), one entry per
subject.}

\item{hinge}{Logical, length 1 or one entry per arm level. Whether
each arm's mean profile changes slope at randomization. \code{FALSE}
for the reference arm constrains its post-randomization slope to
equal the run-in slope (\code{beta = delta}), which makes the run-in
observations directly informative about the control slope. See
Details.}

\item{reference}{The arm level treated as the reference (control).
Defaults to the first sorted level.}

\item{common_close}{Character. \code{"revert"} (the default) sets the
treatment column to zero during the common close, following
\code{res/04-runin-power-analysis}. \code{"retain"} holds the accumulated
treatment contribution constant after the last on-treatment visit.}
}
\value{
A data frame with \code{n * nrow(schedule)} rows and columns
\code{id}, \code{arm}, \code{index}, \code{time}, \code{phase}, and the model columns
\code{x_slope} (the common slope on every visit), \code{x_hinge} (the
reference arm's post-randomization slope increment, present only
when the reference arm is hinged), and one \verb{x_trt_<level>} column
per non-reference arm.
}
\description{
Constructs the three slope columns of the model
`Y_ij = alpha + a_i + (delta (1 - h_j) + beta h_j + gamma h_j g_i
\itemize{
\item b_i) t_j + w_ij`, expanded over subjects.
}
}
\details{
The \code{common_close} argument selects how the treatment column behaves
after the last on-treatment visit. The two conventions are not
equivalent and give different estimands; see Details.

Under \code{"revert"} the treatment indicator is replaced by
\verb{g_i * I(1 <= j <= J1)}, so the mean for a treated subject returns to
the control trajectory at the start of the common close. This is a
discontinuity in the mean: the accumulated benefit \verb{gamma * t_\{J1\}}
is removed at once. It is the convention under which the closed-form
variance results of the companion compendium were derived, and it
makes the common-close visits informative about \code{beta} and the
variance components while contributing nothing to \code{gamma}.

Under \code{"retain"} the treatment column is held at its value at the
last on-treatment visit, so both arms progress with slope \code{beta}
thereafter while the treated arm keeps its accumulated advantage.
This is the more common clinical reading of an off-treatment
follow-up. It is offered because the choice is a modeling decision
rather than a detail, and it should be made explicitly.

The slope for a subject in arm \code{k} is
\itemize{
\item run-in (\code{j <= 0}): \code{delta}, common to all arms;
\item treatment (\verb{1 <= j <= J1}): \code{delta + theta_ref + theta_k};
\item common close (\code{j > J1}): \code{delta + theta_ref}, all arms.
}
\code{theta_ref} is the reference arm's slope change at randomization and
is dropped when \code{hinge} is \code{FALSE} for the reference, constraining
that arm to a single straight line through baseline. \code{theta_k} is
arm \code{k}'s increment over the reference and is the treatment effect
on the rate of change. This is a reparameterization of the
\code{delta}/\code{beta}/\code{gamma} form: \code{theta_ref = beta - delta} and
\code{theta_k = gamma}, spanning the same column space when every arm is
hinged.
}
