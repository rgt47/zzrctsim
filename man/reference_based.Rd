% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing.R
\name{reference_based}
\alias{reference_based}
\title{Reference-based post-discontinuation trajectories}
\usage{
reference_based(
  dat,
  mask,
  method = c("j2r", "cir", "cr", "lmcf"),
  trt_cols = NULL
)
}
\arguments{
\item{dat}{Complete data from \code{\link[=generate_outcomes]{generate_outcomes()}}, carrying \code{beta}
and \code{intercept} attributes.}

\item{mask}{A \code{missing_mask} identifying discontinuation. The first
masked visit for each subject is taken as the discontinuation
point.}

\item{method}{Character. \code{"j2r"} jump to reference, \code{"cir"} copy
increments in reference, \code{"cr"} copy reference, \code{"lmcf"} last mean
carried forward.}

\item{trt_cols}{Character vector of treatment-effect columns.
Defaults to columns beginning \code{x_trt_}.}
}
\value{
\code{dat} with \code{y} and \code{mu} modified after discontinuation, and
a \code{discontinued} logical column added.
}
\description{
Modifies the mean trajectory after treatment discontinuation
according to a pattern-mixture assumption. This is a \emph{generator}:
it produces data whose true post-withdrawal behaviour follows the
named assumption, so that analyses which assume something else can
be evaluated for bias.
}
\details{
The reference mean is the fitted mean with every treatment column
set to zero, so it is the trajectory the subject would have followed
in the control arm. Modifications are applied to the mean and the
subject's realized residual is preserved, which keeps the
within-subject correlation intact.

Note that discontinuation and missingness are distinct: under a
treatment-policy estimand post-discontinuation values are observed,
under a hypothetical estimand they are not. This function alters the
trajectory only. Use \code{\link[=apply_mask]{apply_mask()}} separately if the values are
also to be hidden.
}
