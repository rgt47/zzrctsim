% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgm.R
\name{dgm_conditional}
\alias{dgm_conditional}
\alias{dgm_marginal}
\title{Specify a data-generating mechanism}
\usage{
dgm_conditional(
  G,
  sigma2 = NULL,
  z = function(t) cbind(1, t),
  family = stats::gaussian(),
  trials = 1L,
  theta = NULL
)

dgm_marginal(V, times = NULL)
}
\arguments{
\item{G}{Random-effects covariance, a \verb{q x q} matrix (or scalar for
a single random effect).}

\item{sigma2}{Residual variance. Required for the Gaussian family
and ignored otherwise, since the conditional variance of the other
families is determined by their mean.}

\item{z}{Function of a time vector returning the \verb{n x q}
random-effects design. Defaults to a random intercept and slope,
\code{cbind(1, t)}.}

\item{family}{A \code{\link[stats:family]{stats::family()}} object giving the conditional
distribution of the response given the random effects, and the
link. Supported: \code{gaussian}, \code{binomial}, \code{poisson}, and
\code{MASS::negative.binomial}.}

\item{trials}{Integer. Number of Bernoulli trials per observation
for the binomial family. \code{1} gives a binary outcome.}

\item{theta}{Numeric. Dispersion for the negative binomial, in the
\code{size} parameterization of \code{\link[stats:rnbinom]{stats::rnbinom()}}.}

\item{V}{Either a covariance matrix, in which case \code{times} must be
supplied to say which grid it refers to, or a function of a time
vector returning a covariance matrix.}

\item{times}{Numeric vector. The reference grid, required when \code{V}
is a matrix.}
}
\value{
An object of class \code{dgm}, with subclass
\code{dgm_conditional} or \code{dgm_marginal}.
}
\description{
\code{dgm_conditional()} declares random effects; \code{dgm_marginal()}
declares the response covariance directly. Both produce a \code{dgm}
object that answers \code{\link[=cov_at]{cov_at()}}.
}
\details{
With a non-Gaussian family the response is drawn conditionally on
the random effects: \code{b_i ~ N(0, G)}, then
\code{y_ij ~ family(g^-1(eta_ij))} with
\verb{eta_ij = x_ij'beta + z_ij'b_i}. Two consequences follow and are
the user's to manage rather than the package's to hide.

First, dependence between visits arises solely through the shared
\code{b_i}, so the reachable dependence is exactly what the
random-effects structure can induce. There is no marginal escape
hatch: \code{\link[=dgm_marginal]{dgm_marginal()}} has no meaning outside the Gaussian case.

Second, under a non-identity link the marginal mean is not
\verb{g^-1(x'beta)}. The fixed effects are conditional on the random
effects, so a conditional and a marginal treatment effect differ in
magnitude. An \code{\link[=estimand]{estimand()}} used with a non-Gaussian family must
state which of the two it refers to.
}
