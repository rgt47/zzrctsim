% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/certify.R
\name{reach_error}
\alias{reach_error}
\title{Error incurred by a restricted random-effects budget}
\usage{
reach_error(V, q = NULL)
}
\arguments{
\item{V}{Target covariance.}

\item{q}{Integer vector of random-effect budgets to evaluate.
Defaults to \code{1:(p-1)}.}
}
\value{
A data frame with columns \code{q}, \code{sigma2}, \code{max_abs_error},
and \code{rel_frobenius}.
}
\description{
Best attainable rank-\code{q} plus \verb{sigma^2 I} approximation to a target
covariance, and the resulting discrepancy. Converts "unreachable"
into a statement of how far wrong a given budget would be.
}
\details{
The bound is optimistic for a \emph{random intercept} specifically: it
uses the leading eigenvector of \code{V}, whereas a random intercept is
forced to the all-ones direction and can only do worse or equal.
}
\examples{
ar1 <- 0.6 ^ abs(outer(1:6, 1:6, "-"))
reach_error(ar1)
}
