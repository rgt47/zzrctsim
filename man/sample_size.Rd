% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{sample_size}
\alias{sample_size}
\title{Sample size for a target power, by simulation}
\usage{
sample_size(target = 0.8, n_grid, confirm_B = 2000L, ...)
}
\arguments{
\item{target}{Numeric. Desired power.}

\item{n_grid}{Integer vector. Sample sizes at which to evaluate the
curve. Should bracket the answer.}

\item{confirm_B}{Integer. Replicates for the confirmation run at the
selected size. \code{0} skips confirmation.}

\item{...}{Passed to \code{\link[=sim_power]{sim_power()}}.}
}
\value{
An object of class \code{sample_size}, a list with
\describe{
\item{n_per_arm}{integer. The selected per-arm sample size:
\code{ceiling()} of the interpolated size, or the smallest value on
\code{n_grid} when \code{target} was already exceeded there, in which
case a warning states that it is only an upper bound.}
\item{target}{the requested power, as supplied}
\item{curve}{the \code{power_curve} the selection was interpolated
from}
\item{confirmation}{a one-row \code{\link[=sim_power]{sim_power()}} data frame at
\code{n_per_arm}, run with \code{confirm_B} replicates on an independent
RNG stream, or \code{NULL} when \code{confirm_B = 0}. Its \code{power} and
\code{mcse} are the figures to quote.}
}
}
\description{
Searches for the smallest per-arm sample size attaining \code{power}.
A monotone interpolation is fitted through a simulated power curve
and inverted, then the answer is confirmed by a longer run at the
selected size.
}
\details{
The returned sample size inherits Monte Carlo error from the curve.
The confirmation run reports the achieved power with its MCSE at the
chosen size, which is the number to quote. If the confirmation MCSE
is large relative to the distance from \code{target}, raise \code{B}; see
\code{\link[=nsim_for_mcse]{nsim_for_mcse()}}.

The confirmation run uses a different RNG stream from the curve
(the curve's seed, offset by one), so that it is an independent
check of the selected size rather than a re-reading of the
replicates that selected it.
}
