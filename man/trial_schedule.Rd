% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schedule.R
\name{trial_schedule}
\alias{trial_schedule}
\title{Construct a trial visit schedule}
\usage{
trial_schedule(
  run_in = 0L,
  treatment = 4L,
  common_close = 0L,
  interval = 1,
  times = NULL,
  treatment_end = NULL
)
}
\arguments{
\item{run_in}{Integer. Number of pre-randomization visits (\code{J0}).}

\item{treatment}{Integer. Number of post-randomization on-treatment
visits (\code{J1}).}

\item{common_close}{Integer. Number of post-treatment visits during
which all arms are off treatment (\code{J2}).}

\item{interval}{Numeric. Spacing between visits, in the time unit of
the study. Ignored when \code{times} is supplied.}

\item{times}{Numeric vector. Explicit visit times for unequally
spaced designs. Must contain \code{0}, the randomization visit. When
supplied, \code{treatment_end} determines where the common close begins.}

\item{treatment_end}{Numeric. The time of the last on-treatment
visit. Required with \code{times} when a common close is present;
defaults to the final time, i.e. no common close.}
}
\value{
An object of class \code{trial_schedule}: a data frame with one
row per visit and columns
\describe{
\item{index}{visit index \code{j}, negative during run-in, \code{0} at
randomization}
\item{time}{visit time \code{t_j}, negative during run-in}
\item{phase}{factor: \code{run_in}, \code{randomization}, \code{treatment},
\code{common_close}}
\item{h}{phase indicator, \code{0} for \code{j <= 0} and \code{1} for \code{j > 0}}
\item{on_treatment}{logical, \code{TRUE} for \verb{1 <= j <= J1}}
}
with attributes \code{J0}, \code{J1}, \code{J2}, and \code{interval}.
}
\description{
Defines the observation schedule for a longitudinal trial spanning up
to three phases. Either give the phase counts with a common spacing,
or give explicit visit times for an unequally spaced design.
}
