% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tte.R
\name{tte_from_trajectory}
\alias{tte_from_trajectory}
\title{Derive a time-to-event endpoint from a longitudinal trajectory}
\usage{
tte_from_trajectory(
  dat,
  threshold,
  direction = c("above", "below"),
  interpolate = FALSE
)
}
\arguments{
\item{dat}{Long-format data from \code{\link[=generate_outcomes]{generate_outcomes()}}.}

\item{threshold}{Numeric. The crossing value.}

\item{direction}{Character. \code{"above"} treats crossing upward as the
event, \code{"below"} downward.}

\item{interpolate}{Logical. Linearly interpolate the crossing time
between the bracketing visits, rather than using the visit time
itself. Interval censoring at visit times is the honest default;
interpolation is offered for comparison.}
}
\value{
One row per subject with \code{id}, \code{time}, \code{status}.
}
\description{
Defines the event as the first visit at which a generated
longitudinal response crosses a threshold, so that a survival
endpoint and a continuous endpoint describe the \emph{same} underlying
process rather than being drawn independently.
}
\details{
This is what compendium 13 requires: a latent continuous severity
process from which time to threshold crossing is derived. Because
both endpoints come from one trajectory, a comparison of MMRM
against survival analysis is a comparison of \emph{analyses}, not of two
unrelated data-generating mechanisms.

Subjects never crossing are censored at their last observed visit,
so dropout and administrative censoring both propagate correctly.
}
