% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{apply_dropout}
\alias{apply_dropout}
\title{Apply monotone dropout to generated data}
\usage{
apply_dropout(
  dat,
  target = 0.25,
  psi0 = NULL,
  psi1 = 0,
  psi2 = 0,
  center = NULL,
  from = 0
)
}
\arguments{
\item{dat}{Output of \code{\link[=generate_outcomes]{generate_outcomes()}}.}

\item{target}{Numeric in \verb{(0, 1)}. Target proportion of subjects who
ever drop out. \code{psi0} is calibrated to achieve it. Ignored if
\code{psi0} is supplied.}

\item{psi0}{Numeric. Intercept of the dropout logit. Supply directly
to bypass calibration.}

\item{psi1}{Numeric. Coefficient on the previously observed
response, centred. Non-zero gives MAR.}

\item{psi2}{Numeric. Coefficient on the current, unobserved
response, centred. Non-zero gives MNAR.}

\item{center}{Numeric. Value at which the response is centred in the
hazard. Defaults to the mean of the complete-data response, which
keeps \code{psi0} interpretable as a baseline log-odds.}

\item{from}{Numeric. Dropout is only permitted at visits with
\code{time > from}, so run-in and baseline visits are retained.}
}
\value{
\code{dat} with \code{y} set to \code{NA} at and after each subject's
dropout visit and a \code{dropped} logical column added. Attributes
\code{dropout_spec} (the realized \code{psi0}, \code{psi1}, \code{psi2}, \code{center},
mechanism, target and expected proportion) are attached.
}
\description{
Missingness is applied after complete data are generated, following
the selection-model factorization \verb{f(Y, R) = f(Y) f(R | Y)}, so the
dropout hazard may depend on realized responses.
}
\details{
The hazard follows Diggle and Kenward (1994) \doi{10.2307/2986113}:
for a subject still under observation at visit \code{j},
\deqn{\mathrm{logit}\, P(\mathrm{drop\ at\ } j) =
  \psi_0 + \psi_1 (y_{j-1} - c) + \psi_2 (y_j - c).}
MCAR, MAR and MNAR are the nested restrictions
\code{psi1 = psi2 = 0}, \code{psi2 = 0}, and \code{psi2} free. MNAR therefore
\emph{extends} MAR rather than replacing its history dependence.

Centring at \code{c} matters: on an untransformed ADAS-Cog or CDR-SB
scale an uncentred response saturates the logit, so that any
non-trivial \code{psi1} drives the dropout proportion to one regardless
of the intended level.

Calibration solves for \code{psi0} such that the expected proportion of
subjects ever dropping equals \code{target}. Because the hazards depend
only on complete-data responses, that expectation is available in
closed form as \code{1 - prod(1 - p_j)} averaged over subjects, and is
strictly increasing in \code{psi0}.
}
\examples{
s <- trial_schedule(treatment = 5, interval = 3)
arm <- factor(rep(c("placebo", "active"), each = 50),
              levels = c("placebo", "active"))
d <- runin_design(s, arm, reference = "placebo")
g <- dgm_conditional(G = diag(c(9, 0.04)), sigma2 = 4)
out <- generate_outcomes(d, g,
  beta = c(x_slope = 0.5, x_hinge = -0.1, x_trt_active = -0.25),
  intercept = 20)
mar <- apply_dropout(out, target = 0.25, psi1 = 0.15)
attr(mar, "dropout_spec")$mechanism
}
