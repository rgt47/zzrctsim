% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tte.R
\name{dgm_tte}
\alias{dgm_tte}
\title{Specify a time-to-event data-generating mechanism}
\usage{
dgm_tte(
  shape = 1,
  scale = 12,
  breaks = NULL,
  rates = NULL,
  frailty_sd = 0,
  effect_lag = 0,
  recurrent = FALSE,
  max_events = 100L
)
}
\arguments{
\item{shape}{Numeric. Weibull shape. \code{1} gives an exponential
baseline hazard, values above \code{1} an increasing hazard, below \code{1}
a decreasing one. Ignored when \code{breaks} and \code{rates} are supplied.}

\item{scale}{Numeric. Weibull scale, in the time unit of the study.
With \code{shape = 1} this is the mean time to event at \code{lp = 0}.}

\item{breaks}{Numeric vector of interior cut points for a
piecewise-exponential baseline hazard, e.g. \code{c(6, 12)} for the
intervals \verb{[0, 6)}, \verb{[6, 12)}, \verb{[12, Inf)}. Supply with \code{rates}.}

\item{rates}{Numeric vector of baseline hazard rates, one per
interval, so \code{length(breaks) + 1} of them.}

\item{frailty_sd}{Numeric. Standard deviation of a Gaussian frailty
on the log-hazard scale, \code{b_i ~ N(0, frailty_sd^2)}. Zero gives no
frailty. For recurrent events the frailty is what induces
within-subject dependence between gap times.}

\item{effect_lag}{Numeric. Delay before the covariate effect
begins. Zero gives proportional hazards throughout; a positive
value gives a \strong{non-proportional} hazard in which the arms are
indistinguishable until \code{effect_lag} and diverge thereafter.}

\item{recurrent}{Logical. \code{FALSE} generates time to first event;
\code{TRUE} generates all events in the follow-up window.}

\item{max_events}{Integer. Safety cap on events per subject.}
}
\value{
An object of class \code{c("dgm_tte", "dgm")}, a list with
\describe{
\item{shape, scale}{the Weibull parameters as supplied; ignored
when the baseline is piecewise}
\item{breaks, rates}{the piecewise-exponential specification, or
\code{NULL} for a Weibull baseline}
\item{cuts}{\code{c(0, breaks)}, the left endpoints of the hazard
intervals, or \code{NULL} for a Weibull baseline}
\item{cumH}{baseline cumulative hazard evaluated at \code{cuts},
precomputed so that inversion is a lookup plus one division;
\code{NULL} for a Weibull baseline}
\item{piecewise}{logical, whether the piecewise-exponential
baseline is in force}
\item{frailty_sd}{standard deviation of the log-hazard frailty}
\item{effect_lag}{delay before the covariate effect begins}
\item{recurrent}{logical, whether all events or only the first
are generated}
\item{max_events}{integer safety cap on events per subject}
}
}
\description{
Specify a time-to-event data-generating mechanism
}
\details{
Two baseline hazards are available. The Weibull, with
\code{H_0(t) = (t / scale)^shape} and \code{H_0^{-1}(u) = scale * u^(1/shape)};
and the piecewise exponential, whose cumulative hazard is piecewise
linear and therefore invertible in closed form on each interval. The
piecewise form is the usual choice in trial simulation because it
expresses an arbitrary hazard shape without committing to a
parametric family.

\code{effect_lag} breaks proportional hazards deliberately. The intensity
becomes \verb{h_i(t) = h_0(t) exp(lp_i * I(t > lag))}, so

\if{html}{\out{<div class="sourceCode">}}\preformatted{H_i(t) = H_0(min(t, lag)) +
         [H_0(t) - H_0(min(t, lag))] * exp(lp_i),
}\if{html}{\out{</div>}}

which is still invertible in closed form and so is generated
exactly. A delayed separation of this kind is standard in
immuno-oncology and is the shape anti-amyloid trials in Alzheimer's
disease are usually argued to show; a constant hazard ratio cannot
represent it, and fitting one to such data estimates a
time-averaged quantity that corresponds to no instant of the trial.

A frailty makes the marginal distribution non-proportional even
though the conditional model is proportional hazards, which is the
usual reason a fitted hazard ratio attenuates toward one. That is a
property of the mechanism, not a defect, and it is why the estimand
must state whether it names the conditional or the marginal hazard
ratio.
}
