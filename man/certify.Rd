% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/certify.R
\name{certify}
\alias{certify}
\title{Certify whether a marginal covariance is reachable from random effects}
\usage{
certify(V, tol = 1e-09)
}
\arguments{
\item{V}{A symmetric positive-definite covariance matrix.}

\item{tol}{Numeric. Eigenvalues below this, after shifting, are
treated as zero.}
}
\value{
An object of class \code{reach_certificate}, a list with
\describe{
\item{q_min}{minimum number of random effects}
\item{sigma2}{the residual variance attaining it}
\item{Z}{\verb{p x q_min} matrix of loadings}
\item{G}{\verb{q_min x q_min} diagonal random-effects covariance}
\item{eigenvalues}{spectrum of \code{V}}
\item{p}{dimension}
}
}
\description{
Computes the minimum number of random effects \code{q} required to
represent a target covariance as \verb{Z G Z' + sigma^2 I}, together with
the construction that attains it.
}
\details{
\code{q_min} is \strong{necessary but not sufficient for a particular model}.
The certificate lets \code{Z} be arbitrary, namely the eigenvectors of
\code{V}. A real random-effects specification fixes \code{Z} to design columns
such as an intercept and a slope in time. Compound symmetry has a
flat leading eigenvector, which is why a random intercept reproduces
it exactly; AR(1) has a bowed one, so even the optimal
one-dimensional direction is unavailable to a random intercept.

The certificate therefore proves impossibility cleanly -- no model
with \code{q < q_min} can represent the target -- but does not prove
possibility for a given \code{Z}. Use \code{\link[=reach_error]{reach_error()}} to quantify how
close a given budget can get.
}
\examples{
p <- 6
cs <- matrix(0.5, p, p); diag(cs) <- 1
certify(cs)$q_min          # 1: a random intercept suffices
ar1 <- 0.6 ^ abs(outer(1:p, 1:p, "-"))
certify(ar1)$q_min         # 5: not reachable parsimoniously
}
