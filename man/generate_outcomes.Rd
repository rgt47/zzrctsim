% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{generate_outcomes}
\alias{generate_outcomes}
\title{Generate outcomes for a trial}
\usage{
generate_outcomes(design, dgm, beta, intercept = 0)
}
\arguments{
\item{design}{A data frame of one row per subject-visit, as returned
by \code{\link[=runin_design]{runin_design()}} or by \code{\link[=realize_schedule]{realize_schedule()}} passed through
\code{\link[=runin_design]{runin_design()}}. Must contain \code{id} and \code{time}, plus any columns
named in \code{beta}.}

\item{dgm}{A \code{dgm} object supplying the within-subject covariance.}

\item{beta}{Named numeric vector of fixed effects. Names must match
columns of \code{design}; an \code{"(Intercept)"} entry is added implicitly
if absent from \code{design}.}

\item{intercept}{Numeric. Overall mean at time zero.}
}
\value{
\code{design} with two columns added: \code{mu}, the fixed-effect mean,
and \code{y}, the generated outcome.
}
\description{
Generate outcomes for a trial
}
\details{
Draws are made per subject from a multivariate normal with mean
\code{mu} and covariance \code{cov_at(dgm, times_i)}. Subjects with a single
observation are handled as a univariate draw.
}
\examples{
s <- trial_schedule(run_in = 1, treatment = 4, interval = 3)
arm <- factor(rep(c("placebo", "active"), each = 5),
              levels = c("placebo", "active"))
d <- runin_design(s, arm, reference = "placebo")
g <- dgm_conditional(G = diag(c(4, 0.01)), sigma2 = 2)
out <- generate_outcomes(d, g,
  beta = c(x_slope = 0.5, x_hinge = -0.1, x_trt_active = -0.2))
head(out)
}
